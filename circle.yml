machine:
  php: 
    version: 7.0.7
  timezone: Asia/Tokyo
  services:
    - docker
dependencies:
  pre:
    - pip install docker-compose

test:
  pre:
    - docker-compose --version
    - docker-compose up -d
    - docker-compose run php ls
