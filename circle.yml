machine:
  php: 
    version: 7.0.7
  timezone: Asia/Tokyo
  services:
    - docker

dependencies:
  override:
    - composer install --prefer-source --no-interaction --dev
    - docker-compose up -d
test:
  pre:
    - docker-compose --version
    - docker-compose up -d
    - docker-compose run php ls
